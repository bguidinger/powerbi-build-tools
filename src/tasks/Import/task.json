{
  "id": "DF1CFF5F-8EC0-4102-BC87-C70ABDB3DC51",
  "name": "PowerBI.Import",
  "friendlyName": "Power BI Import",
  "description": "Imports a file to a workspace.",
  "author": "bguidinger",
  "category": "Azure Pipelines",
  "visibility": [
    "Build",
    "Release"
  ],
  "demands": [
    "powershell"
  ],
  "version": {
    "Major": "1",
    "Minor": "0",
    "Patch": "0"
  },
  "instanceNameFormat": "Power BI Import: $(Path)",
  "inputs": [
    {
      "name": "ServiceEndpoint",
      "type": "connectedService:PowerBI",
      "label": "Service Endpoint",
      "required": true
    },
    {
      "name": "WorkspaceName",
      "type": "string",
      "label": "Workspace Name",
      "required": false,
      "helpMarkDown": "Optional: If not specified, your personal workspace will be used."
    },
    {
      "name": "Path",
      "type": "filePath",
      "label": "Path",
      "required": true,
      "helpMarkDown": "The path to the file or folder to import."
    },
    {
      "name": "Conflict",
      "type": "pickList",
      "label": "Conflict",
      "defaultValue": "Abort",
      "required": true,
      "helpMarkDown": "Determines what to do if a file with the same name already exists.",
      "options": {
        "Abort": "Abort",
        "Overwrite": "Overwrite"
      }
    },
    {
      "name": "ConnectionStrings",
      "type": "string",
      "label": "Connection Strings",
      "defaultValue": "",
      "required": false,
      "helpMarkDown": "Connection strings to replace in .rdl files. The keys are the names of the data sources.  The values are the connection strings.",
      "properties": {
        "resizable": "true",
        "rows": "4",
        "editorExtension": "ms.vss-services-azure.azure-servicebus-message-grid"
      }
    }
  ],
  "execution": {
    "PowerShell3": {
      "target": "Run.ps1"
    }
  }
}